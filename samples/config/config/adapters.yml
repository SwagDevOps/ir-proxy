---
x-defaults: &defaults
  dump: true
  logger: true # default: true
  keymap: 'config/keymap.yml'
# adapters -------------------------------------------------------------
dummy:
  <<: *defaults
xdotool:
  options: ["--clearmodifiers"]
  <<: *defaults
